[project]
name = "dogqoder"
version = "0.1.0"
description = "Persistent, observable Qoder workers for Codex"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "qoder-agent-sdk==1.0.13",
]

[tool.uv]
package = false
